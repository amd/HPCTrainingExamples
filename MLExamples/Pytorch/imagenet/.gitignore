# Large generated profiling artifacts (regenerated by the profiling/*.sbatch jobs).
# Only the scripts and the small figs/*.png are tracked.
profiling/roofline/
profiling/tau/
profiling/traces/

# Disposable uv venv + upstream clone left by capture/run jobs.
imagenet_test/

# Job logs and stray backups.
*.log
*.out
*.orig-preaudit*
